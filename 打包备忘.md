# 打包备忘

## 环境
- 使用项目自带虚拟环境：`./.venv/Scripts/python.exe`
- 首次需安装 PyInstaller：

```bash
./.venv/Scripts/python.exe -m pip install pyinstaller
```

## 打包命令（在项目根目录执行）

```bash
./.venv/Scripts/python.exe -m PyInstaller --onefile --noconsole --name pomodoro_app --icon record.ico --add-data "record.ico;." --clean pomodoro_app.py
```

参数说明：
- `--onefile`：打成单个 exe
- `--noconsole`：运行时不弹出黑色控制台窗口（GUI 程序）
- `--name pomodoro_app`：输出 exe 名称
- `--icon record.ico`：设置 exe 的**文件图标**（资源管理器里显示的那个）
- `--add-data "record.ico;."`：把 ico 一并打进 exe，供运行时设置**窗口/任务栏图标**（Windows 下源与目标用 `;` 分隔）
- `--clean`：清理上次构建缓存

## 图标

`record.ico` 内含 256/48/32/16 四种尺寸，三处图标都由它负责：

| 显示位置 | 实现方式 |
| --- | --- |
| exe 文件图标（资源管理器） | 打包参数 `--icon record.ico` |
| 窗口标题栏 / 任务栏 | `App.__init__` 里的 `self.iconbitmap(ICON_PATH)` |

`ICON_PATH` 由 `RESOURCE_DIR = getattr(sys, "_MEIPASS", BASE_DIR)` 拼出：打包后指向 exe 内部的临时解压目录，源码运行时指向脚本同级目录——所以单文件 exe 不需要外置 ico 文件。换图标只需替换 `record.ico` 后重新打包。

> ⚠️ 换了图标重新打包后，如果资源管理器 / 任务栏**仍显示旧图标**，那不是打包失败，而是 **Windows 图标缓存**——覆盖同名 exe 不会让缓存失效。处理办法：停掉 explorer → 删除 `%LOCALAPPDATA%\Microsoft\Windows\Explorer\iconcache*.db` 和 `%LOCALAPPDATA%\IconCache.db` → 重启 explorer。
>
> 快速自检：往桌面**新建一个快捷方式**，它的图标会立刻用上新 exe 的图标；如果快捷方式对了而 exe 文件本身还是旧的，就 100% 是缓存问题。任务栏上若固定过旧版本，固定项缓存独立，需取消固定后重新拖入。

## 产物
- exe 位置：`dist/pomodoro_app.exe`
- 数据目录都在 **exe 同级目录**自动创建（代码里已针对打包做了路径处理）：
  - `pomodoro.db` — 数据库（番茄记录 / 项目 / 标签）
  - `projects/` — 每个项目的笔记文件 `{项目id}.md`
- `build/`、`pomodoro_app.spec` 是中间产物，可删

> ⚠️ 如果你平时就是双击 `dist/pomodoro_app.exe` 记录番茄，那么 `dist/pomodoro.db` 和 `dist/projects/` 就是**真实数据**，不是构建产物——清理 `dist/` 时绝不能删这两个。项目根目录的 `pomodoro.db` 只在你用源码运行时才会生成。

## 也可以直接运行批处理
双击 `build.bat` 即可完成打包。

> ⚠️ `build.bat` 必须保持 **CRLF 行尾**。cmd.exe 解析不了 LF-only 的批处理文件，会把相邻行粘成一条命令并报「不是内部或外部命令」。用编辑器或脚本改过它之后若双击报错，先用 `git ls-files --eol build.bat` 确认工作区行尾是 `w/crlf`。
